import 'package:flutter/material.dart';

class CaNhanPage extends StatelessWidget {
  const CaNhanPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Trang cá nhân", style: TextStyle(color: Colors.white)),
    );
  }
}
