import 'package:flutter/material.dart';

class CongDongPage extends StatelessWidget {
  const CongDongPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Trang cộng đồng", style: TextStyle(color: Colors.white)),
    );
  }
}
