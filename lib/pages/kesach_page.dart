import 'package:flutter/material.dart';

class KeSachPage extends StatelessWidget {
  const KeSachPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Trang kệ sách", style: TextStyle(color: Colors.white)),
    );
  }
}
