import 'package:flutter/material.dart';

class KhamPhaPage extends StatelessWidget {
  const KhamPhaPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Trang khám phá", style: TextStyle(color: Colors.white)),
    );
  }
}
